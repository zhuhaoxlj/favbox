# FavBox Backend Configuration

# Database URL
# SQLite (development): sqlite+aiosqlite:///./favbox.db
# PostgreSQL (production with pgvector): postgresql+asyncpg://favbox:favbox_secure_password_change_in_production@localhost:5432/favbox
DATABASE_URL=postgresql+asyncpg://favbox:favbox_secure_password_change_in_production@localhost:5432/favbox

# Database Backup (SQLite backup after migration)
SQLITE_BACKUP_PATH=./favbox_backup.db

# JWT Configuration
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS Origins (comma-separated)
CORS_ORIGINS=chrome-extension://,moz-extension://,http://localhost:3000

# AI Services
GEMINI_API_KEY=your-gemini-api-key-here

# Proxy (optional - needed if Gemini API is blocked in your region)
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
